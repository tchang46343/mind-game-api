CREATE TABLE users (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    firstName TEXT NOT NULL,
    lastName TEXT NOT NULL,
    email TEXT NOT NULL,
    password VARCHAR(100) NOT NULL
);

CREATE TABLE gameslides (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    word TEXT NOT NULL,
    quote TEXT NOT NULL,
    imageurl TEXT NOT NULL
);